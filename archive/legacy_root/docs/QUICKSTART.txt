# 🚀 QUICK START - OTP REBUILD

## The Problem We Found
✗ OTP loaded graph but NO routing → all endpoints 404
✗ Cause: GTFS files excluded (using `--load` not `--build`)

## The Fix (2 commands)

### Command 1: Rebuild OTP with GTFS (do this ONCE)
```batch
cd /d e:\irish_transport_ai\otp && java -Xmx10G -jar otp-shaded-2.8.1.jar --build graphs/default --serve .
```

**Wait 5-15 minutes** for:
```
Graph service for routers/default is running
```

**KEEP THIS WINDOW OPEN**

### Command 2: Start Chatbot (new window)
```powershell
cd e:\irish_transport_ai
streamlit run dashboard/chat.py
```

Visit: **http://localhost:5501**

## ✅ Success Indicators
- OTP startup shows: "Graph service for routers/default is running"
- Chatbot shows: "Dashboard" in browser
- Routes appear: Real Dublin Bus/Luas names
- Dates work: March-June 2025 (GTFS coverage)

## ⏱️ Time Breakdown
- **First OTP startup**: 10-15 minutes (builds graph with GTFS)
- **Subsequent OTP startups**: 20-30 seconds (uses cached graph)
- **Chatbot startup**: 2-3 seconds
- **Route query**: 1-3 seconds

## 📁 What Got Updated
- `src/llm/tools.py`: Now uses GraphQL API for OTP 2.8.1
- `OTP_REBUILD_WITH_GTFS.md`: Full rebuild instructions
- `FINAL_SETUP_SUMMARY.md`: Complete setup guide

---

**Status**: ✅ Code Ready | ⏳ OTP Needs Rebuild
